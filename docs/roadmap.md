# Development Roadmap

## ✅ Complete

> **Legend:** ✓ = verified working · ◐ = exists but has gaps · ✗ = not yet implemented

### Core Infrastructure

- HTTP server with middleware (request ID, parameter parsing via `ParamBag`)
- Database layer with sqlc-generated type-safe queries — PostgreSQL via pgx
- Configuration system with YAML support, default values, and validation
- Structured logging with request correlation, file logging, numeric level filtering
- CLI framework with dependency injection container (lazy DB, cache, dispatcher, pools)
- Per-tool timeout via `ToolConfig` struct + `runWithTimeout` generic wrapper
- Configurable worker pool size (`consumer.workers`, `enricher.workers`, default 1 each)
- Database-backed batch ownership with heartbeats (`batch_owner` table, stale lease detection, `--force` override, server/CLI isolation via per-process owner UUID)
- Matcher as external process: Hugot embedding model runs as `kushim hugot` over Unix domain socket RPC
- Worker forking: `kushim queue` daemon forks `kushim consume --batch` for processing; `edub` only enqueues tasks (`CGO_ENABLED=0`)
- Matcher RPC protocol: encode, match, consolidate, add-to-store, remove-from-store operations over HTTP/Unix socket
- Queue-driven batch processing: API creates `queued` batches; `kushim queue` consumes them with configurable concurrency (`server.max_concurrent_batches`)
- `internal/configtask/` — ConfigTaskHandler (downloads tessdata/Hugot model in background, registered as "config" task type)
- `internal/utils/files.go` — `ListFilePaths` scans inbox directories with MIME detection (replaced `internal/fileresolver/`)
- Config handler `AdoptBatch` renamed to `ResumeBatch`, now enqueues for queue daemon
- **User accounts**: CRUD with bcrypt password hashing (`golang.org/x/crypto/bcrypt`), user list page in settings UI (tab bar: Configuration / Users), role assignment (admin/editor/viewer), 9 API endpoints, 30+ handler tests
- **`mime_type` → `original_type` rename + viewer simplification**: `document.mime_type` / `orphaned_file.mime_type` columns renamed to `original_type` (migration 00006) and now always store the uploaded file's format (converter no longer overwrites it with `application/pdf`). Removed `MimeInfo.Viewable`/`Viewer`/`OfficeFormat`, `mime.IsViewable()`, `DistinctMimeTypes()`, `GET /api/v1/filter-mime-types` handler+route, and the MIME type filter from search (`SearchFilter.MimeType`) and the UI. Document preview is now always an iframe; `OfficePreview.svelte` and the `officeparser` npm dependency were removed. API JSON keys `mime_type` → `original_type` (dashboard: `mime_type_breakdown` → `original_type_breakdown`).
- **Trash / soft delete** (#80): `document.deleted_at` column (migration 00007) — `DELETE` moves files to `<storage>/trash/<document_id>/{originals,processed}/` and marks the row deleted instead of removing it; all read/dashboard/search/count queries plus `LEFT JOIN` count queries filter `deleted_at IS NULL`; checksum dedup queries exclude trashed rows so re-ingestion is allowed. New trash API (`GET /api/v1/trash`, `GET/POST /api/v1/trash/{id}`, `DELETE /api/v1/trash/{id}`, `POST /api/v1/trash/batch-delete`, `POST /api/v1/trash/batch-restore`, `POST /api/v1/trash/purge`) with per-document restore/permanent-delete and batch variants; `storage.trash.retention_days` config (default 30) drives an hourly background purge (plus manual `POST /api/v1/trash/purge`) that deletes expired rows then sweeps orphaned trash dirs (5-minute grace period against in-flight soft-deletes). Settings page gains a Trash section. Web UI gains a `/trash` page (list soft-deleted documents, restore or permanently delete individually or in batch, trigger manual purge).
- **Restore path rewriting & config preservation**: `kushim restore` now rewrites database storage paths (`document.storage_path`/`original_path`, `orphaned_file.file_path`) from the archived storage directory to the current one after the SQL dump — the archived dir comes from the manifest `storage_dir` field (added to backups), with a fallback to `storage.storage_dir` in the archived config for old backups (`~`-relative paths are skipped with a warning since they're host-dependent). The archived config is no longer overwritten in place: it's saved as `config.yaml.restored` and the current config stays intact. Follow-up tracked as #89 (granular restore flags).

### Document Pipeline

- UUID-based `document_id` column added to document table for external identification and log correlation
- Document consumption pipeline: scan inbox → extract text → OCR fallback → optimize → store
- Text extraction via MuPDF CGo wrapper (default), gopdf (pure Go), or pdftotext (external tool)
- OCR via gosseract (Tesseract + MuPDF at 200 DPI) or ocrmypdf (external tool)
- Searchable PDF generation with invisible text layer (PDF text rendering mode 3)
- PDF optimization via MuPDF (CGo wrapper) or Ghostscript (external tool)
- Database reset capability (`kushim setup --reset-database`) — drops all tables and re-runs schema + seeders
- MD5 → SHA512 two-step duplicate detection
- Dual storage: originals preserved alongside processed/OCR'd versions
- Date-based storage organization (`year/month/day/hour/documentID.ext`)
- Database transaction with rollback on file-operation failure
- Deferred cleanup of temporary files
- Embedded Tesseract language data (`eng.traineddata`)
- OCR subprocess isolation: gosseract adapter forks `kushim internal-ocr` to prevent heartbeat starvation during image-only PDF processing
- **Parallel OCR acceleration**: channel-based worker pool (`ocr_workers` config, 0 = auto) parallelizes Tesseract calls across CPU cores; eliminated double MuPDF render via nearest-neighbor downscale (200→150 DPI); Tesseract tuning knobs (`load_system_dawg`, `load_freq_dawg`, `OEM_LSTM_ONLY`)
- Custom MuPDF 1.28.0 CGo wrapper (document open/close, page rendering, text extraction, `pdf_clean_file`)
- **Raster image ingestion (PNG, JPEG, TIFF)** — Pure Go image decoding (`image.Decode` + `golang.org/x/image/tiff`) with alpha-on-white compositing, routed through the same gosseract OCR subprocess. 50MP dimension limit. Originals preserved with their real extension (`documentID.png`), processed output always `.pdf`.
- **Word-processor ingestion (DOCX, ODT)** — Pure Go text extraction via `archive/zip` + `encoding/xml` parsing of `word/document.xml` (DOCX) or `content.xml` (ODT). CompositeExtractor wraps PDF + DOCX + ODT extractors dispatching by MIME type. Non-PDF files skip PDF optimization and preserve their original extension (`.docx`/`.odt`) when no OCR or optimization occurs. PageCount=0 for native-format documents.
- **DOCX/ODT to PDF conversion** — Optional LibreOffice headless conversion for DOCX and ODT files during ingestion. When `consumer.converter.enabled` is true, office documents are converted to PDF before text extraction, enabling PDF preview, PDF page counting, and PDF optimization. The original is preserved alongside the converted PDF. Disabled by default (opt-in). Adapter in `internal/tools/adapters/converter/`, config at `consumer.converter.{enabled,binary,timeout}`.

### Enrichment Pipeline

- Async enrichment pipeline: consume → enqueue enrich → text reduction → tag matching → LLM classification → tag consolidation
- LLM providers: OpenAI, Anthropic, DeepSeek, Mistral, Qwen, Zhipu (via `ContentAnalyzer` interface)
- Text reduction via TextRank extractive summarization (TF-IDF, weighted PageRank, diversity penalty)
- Semantic tag matching via Hugot (Go or ONNX backend), cosine similarity, chunked encoding
- Dual text reduction: separate `target_words` for LLM and `reduce_target_words` for tag matching
- Post-LLM tag consolidation via `Consolidate` (fixes casing, hyphenation, synonym mismatches)
- **Post-normalization tag filtering** (`FilterTags`) — rule-based validator drops >3-word tags, tags matching person names (from the current document or the full `people` table via strict-subset rule), and tags that restate the title, then caps at `maxTags` (5). Prompt over-requests 8 tags to survive filtering.
- Tag embedding cache (`BuildTagCache`) — pre-computed tag embeddings at startup
- **New tag cache update**: newly created tags during enrichment are immediately encoded and added to the embedding cache
- **Classification persistence**: enrichment writes to `document_tag`, `document_people`, `document.document_type_id` including auto-creating new tags/people as needed
- Seeded tag vocabulary (110+ Dewey Decimal tags via `sql/seed-tags.sql`)
- Seeded document types (`sql/seed-document-types.sql`)
- Seeded people types (`sql/seed-people-types.sql`)
- Token usage stats and prompt logging
- **Server-side title enforcement** — `analysis.Title` rune-capped at 127 via `utils.Truncate` before persistence, preventing degenerate LLM responses from filling the DB with multi-thousand-char titles
- **Empty-analysis soft retry** — an entirely empty LLM analysis (blank title/type/language, empty tags/people) triggers one automatic retry; if still empty, the enrich task fails cleanly for visibility and manual retry instead of storing empty metadata
- **Pre-request LLM token budget check** — `ContentTooLargeError` returned by the content analyzer when estimated tokens exceed `MaxInputTokens`. The enricher catches it, proportionally re-reduces text, and retries once (up to 2 total attempts). Response-time `TokenLimitError` (from provider error body) follows the same retry path. Falls through to a clear "document too large" error if the document can't be reduced enough.
- **Optional fallback LLM providers** — the Runner builds one `ContentAnalyzer` per enabled entry in `enricher.contentanalyzer.fallbacks` (an ordered list, each `{enabled, llm}` with the same shape as `llm`). Provider-attributable failures on the primary (network, HTTP status, credits, provider-side timeout) retry through each enabled fallback in list order in both `AnalyzeContent` and `AnalyzeDocType`; request-side (`ContentTooLargeError`, `TokenLimitError`) and batch-lifecycle (`context.Canceled`/`DeadlineExceeded`) errors never do. Disabled by default. Credit errors pause the batch only when the primary and every enabled fallback fail.

### Search & Retrieval

- PostgreSQL tsvector full-text search via generated column + GIN index
- `ts_rank` relevance ranking, `ts_headline` snippet highlighting
- Automatic index sync via `GENERATED ALWAYS AS` — no manual update needed
- **Structured search** (`POST /api/v1/documents/search`) — dynamic SQL query builder with filters for tags, people, document type, language, date range, file size
- **Search engine** (`internal/search/search.go`) — `Engine.SearchStructured()` returning results + total count
- **Database query builder** (`internal/database/structured_search.go`) — dynamic `WHERE` clause composition with proper parameterization, batch tag/people fetching
- **Autocomplete endpoints** — prefix search for tags (`SearchTagsByName`), people (`SearchPeopleByName`), document types, person types
- **Saved searches** — `saved_search` table, CRUD API, frontend save/load/delete
- **Frontend query parser** (`searchFilter.js`) — tokenizes `field:value` syntax into structured filter state

### Task System

- Generic `task.Dispatcher` + `pool.Pool` with `task.Handler` / `Dedupable` interfaces
- Task lifecycle: `waiting` → `pending` → `processing` → `completed` / `failed` / `cancelled` / `discarded`, plus `discarded` → `waiting` on consume retry/reset
- Linked task pairs: enrich tasks start `waiting` until their consume task completes
- Batch cancellation: cancels pending tasks, sends SIGTERM, marks in-flight as cancelled
- Retry support for failed tasks
- Dedup prevention via unique partial index on active tasks (`task_type`, `dedup_key`)
- **Runner retry + FailTask fallback**: `CompleteTask` retried 3× with exponential backoff on transient errors; all retries exhausted → task is `failed` rather than stuck in `processing`
- **`CompleteTask` status guard**: `UPDATE ... WHERE id = $1 AND status = 'processing'` prevents stale completions from racing with the reclaim sweep
- **Age-based stale task sweep**: generic reclaim in the queue daemon resets `processing` tasks older than `consumer.reclaim.stale_task_after` (default 600s) to `pending`/`failed`, independent of batch ownership
- **Per-task panic recovery in `Runner.Next`**: a deferred `recover()` after the claim fails the in-flight task immediately with the real panic text (`panic: <text>` in `task.error`) and the REQID logged; the error is returned to the pool (logged, no worker restart). The age-based sweep stays as the backstop for hard crashes (kill -9, OOM) where no Go-level recover can run
- **Enforce enrich-task discard + restore on retry**: every consume failure path (early exits included) now discards the paired `waiting` enrich task, with discard failures surfaced in the task's `error` field. Retrying/resetting a failed consume restores its `discarded` enrich to `waiting` (`SetEnrichTaskWaiting` targeted restore; transactional join-based restores in `RetryFailed` and all reset paths). Recovery sweeps (`QuarantineFailedFiles` batch-scoped + global variant in `ResetStaleProcessingTasks`) discard any enrich still `waiting` next to a terminal-failed consume, enforcing "enrich is never waiting while its consume is failed"

### API Endpoints

| Endpoint                               | Description                                                                                                                                                                                                                   |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /health`                          | Health check                                                                                                                                                                                                                  |
| `GET /api/v1/documents`                | List documents (sortable, paginated)                                                                                                                                                                                          |
| `GET /api/v1/documents/{id}`           | Get document with tags, people                                                                                                                                                                                                |
| `GET /api/v1/documents/{id}/file`      | Download PDF file                                                                                                                                                                                                             |
| `GET /api/v1/documents/search`         | tsvector search with snippets                                                                                                                                                                                                 |
| `POST /api/v1/documents/search`        | Structured search (tags, people, dates, size)                                                                                                                                                                                 |
| `PUT /api/v1/documents/{id}`           | Update document metadata (title, type, language)                                                                                                                                                                              |
| `DELETE /api/v1/documents/{id}`        | Soft-delete document + move files to trash (restorable)                                                                                                                                                                       |
| `POST /api/v1/documents/{id}/tags`     | Add tag assignment to document                                                                                                                                                                                                |
| `DELETE /api/v1/documents/{id}/tags`   | Remove tag assignment from document                                                                                                                                                                                           |
| `POST /api/v1/documents/{id}/people`   | Add person assignment to document                                                                                                                                                                                             |
| `DELETE /api/v1/documents/{id}/people` | Remove person assignment from document                                                                                                                                                                                        |
| `POST /api/v1/documents/{id}/reenrich` | Re-run enrichment for a single document (creates queued batch)                                                                                                                                                                |
| `POST /api/v1/documents/batch-delete`  | Batch soft-delete documents (with partial failure reporting)                                                                                                                                                                  |
| `POST /api/v1/documents/batch-tags`    | Batch assign tags (add or replace mode, with transaction)                                                                                                                                                                     |
| `GET /api/v1/tags`                     | List/autocomplete tag names (`?q=`, `limit`, `offset`)                                                                                                                                                                        |
| `POST /api/v1/tags`                    | Create tag                                                                                                                                                                                                                    |
| `PUT /api/v1/tags/{id}`                | Update tag name                                                                                                                                                                                                               |
| `DELETE /api/v1/tags/{id}`             | Delete tag                                                                                                                                                                                                                    |
| `GET /api/v1/people`                   | List/autocomplete people names (`?q=`, `limit`, `offset`)                                                                                                                                                                     |
| `POST /api/v1/people`                  | Create person                                                                                                                                                                                                                 |
| `PUT /api/v1/people/{id}`              | Update person name, native name                                                                                                                                                                                               |
| `DELETE /api/v1/people/{id}`           | Delete person (CASCADE to document_people)                                                                                                                                                                                    |
| `GET /api/v1/people-types`             | List/autocomplete person types (`?q=`, `limit`, `offset`)                                                                                                                                                                     |
| `POST /api/v1/people-types`            | Create person type                                                                                                                                                                                                            |
| `PUT /api/v1/people-types/{id}`        | Update person type name, description                                                                                                                                                                                          |
| `DELETE /api/v1/people-types/{id}`     | Delete person type (409 if referenced)                                                                                                                                                                                        |
| `GET /api/v1/document-types`           | List/autocomplete document types (`?q=`, `limit`, `offset`)                                                                                                                                                                   |
| `POST /api/v1/document-types`          | Create document type with description                                                                                                                                                                                         |
| `PUT /api/v1/document-types/{id}`      | Update document type name, description                                                                                                                                                                                        |
| `DELETE /api/v1/document-types/{id}`   | Delete document type (409 if referenced)                                                                                                                                                                                      |
| `GET /api/v1/saved-searches`           | List saved searches                                                                                                                                                                                                           |
| `POST /api/v1/saved-searches`          | Create saved search                                                                                                                                                                                                           |
| `DELETE /api/v1/saved-searches/{id}`   | Delete saved search                                                                                                                                                                                                           |
| `GET /api/v1/users`                    | List users (paginated, excludes password_hash/api_key_hash)                                                                                                                                                                   |
| `GET /api/v1/users/{id}`               | Get single user                                                                                                                                                                                                               |
| `POST /api/v1/users`                   | Create user (username + bcrypt password, optional role, defaults to viewer)                                                                                                                                                   |
| `PUT /api/v1/users/{id}`               | Update username + optional password + optional role                                                                                                                                                                           |
| `DELETE /api/v1/users/{id}`            | Delete user                                                                                                                                                                                                                   |
| `POST /api/v1/users/{id}/api-key`      | Generate API key (returns raw key once, 201)                                                                                                                                                                                  |
| `DELETE /api/v1/users/{id}/api-key`    | Revoke API key (204)                                                                                                                                                                                                          |
| `PUT /api/v1/users/{id}/api-key`       | Rotate API key (overwrites, returns new key, 200)                                                                                                                                                                             |
| `GET /api/v1/users/{id}/api-key`       | Get API key status (has_api_key, prefix, created_at)                                                                                                                                                                          |
| `POST /api/v1/auth/login`              | Authenticate user, return JWT token + user profile (includes role), set `edub_token` HttpOnly cookie                                                                                                                          |
| `POST /api/v1/auth/logout`             | Clears `edub_token` cookie + client-side token discard                                                                                                                                                                        |
| `GET /api/v1/me`                       | Get current user profile (includes role)                                                                                                                                                                                      |
| `POST /api/v1/me/api-key`              | Generate own API key (self-service, 201)                                                                                                                                                                                      |
| `DELETE /api/v1/me/api-key`            | Revoke own API key (self-service, 204)                                                                                                                                                                                        |
| `PUT /api/v1/me/api-key`               | Rotate own API key (self-service, 200)                                                                                                                                                                                        |
| `GET /api/v1/me/api-key`               | Get own API key status (self-service)                                                                                                                                                                                         |
| `POST /api/v1/consume`                 | Scan inbox, enqueue files, create queued batch                                                                                                                                                                                |
| `POST /api/v1/consume/upload`          | Upload files via multipart, create queued batch                                                                                                                                                                               |
| `GET /api/v1/tasks`                    | List tasks (batch, status filters)                                                                                                                                                                                            |
| `GET /api/v1/tasks/{id}`               | Get single task                                                                                                                                                                                                               |
| `GET /api/v1/batches`                  | List batch summaries                                                                                                                                                                                                          |
| `GET /api/v1/batches/{id}`             | Get single batch summary                                                                                                                                                                                                      |
| `POST /api/v1/batches/{id}/resume`     | Resume batch (enqueues for queue daemon)                                                                                                                                                                                      |
| `POST /api/v1/batches/{id}/cancel`     | Cancel batch (SIGTERM worker, mark tasks cancelled)                                                                                                                                                                           |
| `POST /api/v1/batches/{id}/retry`      | Retry all failed tasks in a batch (reset to pending)                                                                                                                                                                          |
| `POST /api/v1/documents/download`      | Download multiple documents as a ZIP archive                                                                                                                                                                                  |
| `GET /api/v1/filter-languages`         | List available languages for structured search filter dropdown                                                                                                                                                                |
| `GET /api/v1/supported-mime-types`     | List configured MIME types with metadata (label, extension) for the frontend                                                                                                                                                  |
| `GET /api/v1/dashboard`                | Dashboard data: recent batches + running tasks (active tasks strip, processing-first) + document analytics (top-10 language distribution, type/tag distributions, missing counts) + storage panel (totals, original type breakdown, storage trend, pages, words) |
| `GET /api/v1/errored`                  | List errored files from disk (`errors/` and `errors/duplicated/`)                                                                                                                                                             |
| `GET /api/v1/errored/download`         | Download single errored file (with path traversal guard)                                                                                                                                                                      |
| `DELETE /api/v1/errored`               | Delete single errored file                                                                                                                                                                                                    |
| `POST /api/v1/errored/delete-all`      | Delete all errored files                                                                                                                                                                                                      |
| `GET /api/v1/logs/{name}`              | Read log file: whitelisted names (kushim, edub, hugot, queue), `?lines=N` param (100–5000), tail-read for >2 MiB, multi-line entry merge via timestamp prefix detection                                                       |
| `GET /api/v1/trash`                    | List trashed (soft-deleted) documents, paginated, ordered by `deleted_at` DESC                                                                                                                                                |
| `GET /api/v1/trash/{id}`               | Get single trashed document metadata (no filesystem paths)                                                                                                                                                                    |
| `POST /api/v1/trash/{id}/restore`      | Restore a trashed document (moves files back, clears `deleted_at`)                                                                                                                                                            |
| `DELETE /api/v1/trash/{id}`            | Permanently delete a trashed document (row + trash files)                                                                                                                                                                     |
| `POST /api/v1/trash/batch-delete`      | Batch permanently delete trashed documents (with partial failure reporting)                                                                                                                                                   |
| `POST /api/v1/trash/batch-restore`     | Batch restore trashed documents (with partial failure reporting)                                                                                                                                                              |
| `POST /api/v1/trash/purge`             | Permanently delete all trashed documents older than `storage.trash.retention_days` (also runs hourly in background)                                                                                                           |
| `GET /api/v1/llm/models`               | List available adapters, providers, and models from the model catalog capability registry                                                                                                                                     |

### CLI Commands

| Command                       | Description                                                                                   |
| ----------------------------- | --------------------------------------------------------------------------------------------- |
| `kushim setup`                | Generate config, download models, init DB                                                     |
| `kushim consume`              | Scan inbox, enqueue, direct-fallback if queue empty                                           |
| `kushim consume --batch <id>` | Resume existing batch                                                                         |
| `kushim consume --batch <id>` | Resume existing batch                                                                         |
| `kushim consume --force`      | Override stale PID lock                                                                       |
| `kushim consume cancel <id>`  | Cancel running batch                                                                          |
| `kushim enrich <id>`          | Re-run enrichment for a single document                                                       |
| `kushim thumbnails`           | Backfill missing thumbnails (`--all`, `--batch <id>`, `--document <id>`)                      |
| `kushim search`               | tsvector search with ANSI highlighting                                                        |
| `kushim task list`            | List tasks with filters                                                                       |
| `kushim task status <id>`     | Show task details                                                                             |
| `kushim task retry <id>`      | Reset failed task to pending                                                                  |
| `kushim user create`          | Create a user with role assignment via CLI                                                    |
| `kushim version`              | Print version                                                                                 |
| `kushim hugot`                | Start matcher RPC server over Unix socket                                                     |
| `kushim backup`               | Create a backup (full/database/documents via `--mode`) of database, config, and storage files |
| `kushim config`               | View and edit config values in the terminal                                                   |
| `kushim restore`              | Restore from a backup archive                                                                 |
| `edub`                        | Start API server                                                                              |
| `edub version`                | Print server version                                                                          |

### Web UI

- SvelteKit SPA with embedded Go binary (`//go:embed`)
- CSS custom properties design system (clay/gold/lapis/parchment palette)
- Reusable `DataTable` component (sortable columns, paginated, configurable page sizes, total count)
- API client module (`src/lib/api.js`) — documents, tasks, batches, summary, health, autocomplete, saved searches
- Hot-reload dev server (`npm run dev`), production build via `make web-build`
- **Structured search UI**: `SearchBar.svelte` with chip display and autocomplete suggestions
- **Filter panel**: `FilterPanel.svelte` — collapsible panel with tags, people, document type, language, date range, file size filters
- **Filter state management**: shared reactive store (`filterStore.js`) + query parser (`searchFilter.js`)
- **Saved searches**: save/load/delete search configurations via API
- **DataTable skeleton loading**: loading state shows animated skeleton rows on every fetch (initial, pagination, sort, search), not just empty initial load (#65)
- **StorageTrend responsive SVG**: cumulative storage trend graph fills container width; removed hard `max-width` cap (#67)
- **People page DataTable**: replaced hand-written table with reusable DataTable component with pagination, sorting, search filter, and count envelope (#68)
- **"warn" log level option in settings UI** (#69): added `warn` as a selectable log level in the settings page, between "error" and "info"
- **PDF optimizer timeout allows 0** (#70): changed both wizard and settings page PDF timeout input `min="1"` to `min="0"` so users can disable the timeout
- **Removed deprecated "Delete original files" from wizard** (#71): removed the `consumer.delete_original` checkbox from the setup wizard (already absent from settings page and backend)
- **MaxConcurrentBatches in settings UI** (#72): added number input for `server.max_concurrent_batches` in the server configuration section
- **Fix blank max download fields** (#73): exposed `server.max_download_files` and `server.max_download_size_mb` in the API `ConfigResponse` so the settings page inputs are pre-filled
- **PostgreSQL SSL mode dropdown** (#74): replaced free-text SSL mode input with a `<select>` dropdown in both wizard and settings (disable/allow/prefer/require/verify-ca/verify-full)
- **Separate database section from storage** (#75): split combined "Storage & Database" section into dedicated "Storage" and "Database" sections in both UIs
- **Timeout=0 for TextExtractor, OCR, TextReducer, TagMatcher** (#76): extended the `min="0"` timeout pattern to all four remaining consumer/enricher timeout inputs so users can disable per-component deadlines. TextReducer and TagMatcher now actually enforce their config timeout (was dead code). Config validation rejects negative values.
- **Converter + supported file types in wizard & settings UI** — exposed `consumer.supported_files`, `consumer.converter.*`, and a new `available_file_types` list (PDF marked required) in the API `ConfigResponse`. Both the setup wizard (step 2) and the settings page Consumer section now render file-type checkboxes (PDF locked on) and LibreOffice DOCX/ODT converter controls (enabled, binary, timeout). Extension aliases (`.jpeg`, `.tif`) centralized in `internal/mime` via `ExtensionsFor()`.
- **Thumbnails section in settings UI** — exposed `consumer.thumbnail.*` (enabled, engine, dpi, max_width, quality, timeout, workers, backfill_interval, backfill_time) in the API `ConfigResponse` and added a Thumbnails section to the settings page between Consumer and Polling, with an engine dropdown bound to `available_engines.thumbnail` (mupdf only) and a normalizer that backfills defaults for configs predating the feature.
- **Dashboard analytics panel: accessibility & responsive layout pass** — Top Tags converted from a table to a div-based list matching the language/doc-type distribution rows; all analytics filter links (missing language/type, untagged, tag/language/document-type rows) now carry the full filtered URL in `href` (Cmd/Ctrl-click and open-in-new-tab land on the filtered documents view; SvelteKit handles SPA navigation), replacing the `onclick` + `goto()` interception pattern; decorative color swatches marked `aria-hidden`; distribution bars animate `transform: scaleX` instead of `width` and skip animation under `prefers-reduced-motion` (`motion-safe:transition-transform`). Processing health panel wrapper and settings page sections unified to consistent spacing (`space-y-6` / `mb-3`).
- **Dashboard: Active Tasks strip (replaces Recent Activity)** — the low-signal activity timeline (document/task/batch events) was replaced by a horizontally scrollable strip of live tasks (`pending`/`processing`/`waiting`, all task types, processing-first then oldest-queued, capped at 25). Backed by a single sqlc query `ListActiveTasks`; the dashboard response now carries `running_tasks[]` (with a derived human-readable `label` per task: `TaskType: file name` for consume/enrich/thumbnail, task-type/dedup-key based for backup/config, e.g. "Consume: report.pdf", "Backup (full)", "Database migration", "Download tessdata (eng)"). Cards link to `/tasks/<task_id>`; a "View all N →" overflow link opens `/tasks?status=active`, a new paginated task-table view (URL-driven `?status=` filter) with a back link. Activity infra (`ListActivityTimeline`, `ActivityEvent`, `ActivityTimeline.svelte`) removed; `formatRelative` + shared `statusChip.js` utilities added; strip is an `aria-live="polite"` region.
- **Dashboard click-through fix** — dashboard analytics links now apply their filter correctly on the documents page. Two defects fixed: (1) the URL filter (`?q=`) is applied via `fromQueryString` in the documents page script body _before_ `filterStore.subscribe`, eliminating the stale-load race where `DataTable`'s `onMount` fired its first search with the previous visit's store state (the old `onMount`-based apply caused two concurrent `POST /api/v1/documents/search` requests); (2) `serializeFilter` always wraps field values in double quotes (`tag:"x"`, `type:"scientific paper"`), so multi-word tag/type/lang/person values survive the URL round-trip instead of being split by the query tokenizer (`tag:social change` previously became `tags:["social"]` + free-text `"change"`). The document-types page builds its `/documents?q=` links through `serializeFilter` for the same quoting.

#### Web UI — Page Status

| Route                 | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/` (dashboard)       | ✓      | Health + summary stats + recent docs + batch overview panel + active tasks strip (pending/processing/waiting cards, processing-first, "View all" → `/tasks?status=active`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `/documents`          | ✓      | Structured search bar + filter panel + saved searches + sortable DataTable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/documents/[id]`     | ✓      | PDF iframe preview + editable metadata sidebar (title, type, language, tags, people) + delete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/settings`           | ✓      | Full configuration form: server host/port/max upload/max download files/max download size/max concurrent batches/max batch delete/auth enabled toggle, SSL mode dropdown, OCR, consumer, thumbnails (enabled, engine, dpi, max width, quality, timeout, workers, backfill interval, backfill time), polling (enabled, interval, active windows), reclaim (enabled, max retries, stale task after), text extractor, PDF optimizer (timeout min 0), enricher, content analyzer (LLM) with cascading adapter/provider/model selectors + token + temperature + reasoning + timeout + pause on credit error + doc type refinement (enabled, head words, tail words) + fallback LLM sub-card (enable toggle + cascading adapter/provider/model + token + temperature + request delay), tag matcher, text reducer, backup (enabled, fallback path, per-mode schedule list with add/remove/edit), separate Storage & Database sections, Trash section (retention days), "warn" log level, Users tab (DataTable + create/edit/delete modals), logout confirmation, unsaved-changes warning |
| `/login`              | ✓      | Centered card form with username/password fields, auth-live error display, loading spinner, redirects to dashboard on success                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/tags`               | ✓      | List, filter, create, edit, delete tags                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/people`             | ✓      | Two tabs: People (name + native name) and Person Types (name + description)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `/document-types`     | ✓      | List, create, edit, delete document types                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `/tasks`              | ✓      | Batch list + task drill-down + status-filtered task table (`?status=active` from the dashboard strip, any literal status via URL)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `/tasks/[id]`         | ✓      | Task detail with status badge, batch/doc links, timestamps, error display, retry action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/documents/orphaned` | ✓      | Two tabs: Orphaned (scan, list, delete, restore, move-to-inbox) and Errored (list, download, delete, delete-all from disk)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/trash`              | ✓      | DataTable of soft-deleted documents (fixed `deleted_at` desc order): per-row restore / permanent delete, batch restore / permanent delete via row selection, purge-expired button                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `/logs`               | ✓      | Tabbed log viewer (Kushim, Edub, Hugot, Queue), lines control, auto-refresh, monospace color-coded rendering with expandable long lines, multi-line entry merge, jump-to-bottom                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### Quality

- ✓ Database integration tests (35+ tests) — document/tag/people CRUD, task lifecycle (incl. enrich discard/restore/sweep queries), enrich waiting flow, batch ownership, paused batch counting/listing, structured search, analytics queries (including language distribution LIMIT 10), saved searches, backup lock lifecycle, gated task claiming, people count queries
- ✓ Search engine tests (8 tests) — tsvector search, structured search (language/missing filters), pagination, query sanitization
- ✓ Task system tests (30 tests) — Store, dispatcher, runner, nil payload handling, backup lock gating, pool lifecycle, dedup key handling, gated task claiming, `Retry` restore, consume-handler early-exit discards
- ✓ API handler tests (60+ tests) — health, document CRUD, tag/people CRUD, user CRUD, task endpoints, saved searches, concurrent operations, auth login/logout, token claims, errored file list/download/delete/delete-all, logs viewer (invalid name, file not found, success, lines clamping, large file tail, empty file), API key generate/revoke/rotate/status, ReEnrich handler (success + not found), orphaned handler (list/scan/delete/restore/move-to-inbox/delete-all), errored handler (list/download/delete/delete-all)
- ✓ Auth package tests (7 tests) — session secret generation, JWT generation/validation, wrong secret, expired token, malformed token, ValidRole
- ✓ Auth middleware + permission tests (24 tests) — public path bypass, missing/invalid/valid token, wrong secret, missing bearer prefix, empty header, disabled flag passes all paths, valid API key, invalid API key, wrong prefix falls through, auth disabled bypasses, internal error returns 500, valid/invalid cookie, header-priority, RequireRole admin/editor/viewer (allows+forbids), missing role, invalid role
- ✓ Service layer tests (80+ tests) — Batch create/get/owner-state/pending/active/cancel/queue/paused, retry-failed restore, reset/stale-task restore + sweep, orphaned scan/delete/restore, errored files list/download/delete/delete-all, user API key create/revoke/rotate/validate, user Create with role, UpdateRole, Update with role change, password validation (12+ rules), people count/by-name queries
- ✓ Tagmatch tests (3 tests) — MaxMatchBodyBytes derivation, UTF-8 truncation, idempotency
- ✓ Storage tests (7 tests) — DetectFileType, WalkStorageDir (finds PDFs, skips non-PDF/recent/unknown key types/missing dirs), QuarantineFile, RemoveOrphanedFile, CopyToConsumptionDir
- ✓ Consumption pipeline tests (21 tests) — full consume flow with mock runner, file I/O, duplicate detection, error paths, orphaned file management, paused-batch scan guard
- ✓ LLM registry tests (27 tests) — fixture/embedded catalog, model lookup, adapter/provider listing, default URL, concurrent access
- ✓ Config tests — defaults, ParseHHMM, polling windows, Load (OcrWorkers), SaveMap, watcher lifecycle
- ✓ Utils tests (23 tests) — Truncate (ASCII/CJK/mixed), EstimateTokens, NormalizeForDB (accent folding/punctuation/spaces), NormalizeForDB_Concurrent (8-goroutine `-race` regression guard for the shared-`transform.Chain` data race), logger level parsing/gating/formatting/file output/SlogLogger bridge
- ✓ Content analyzer tests (16 tests) — NormalizeTags, BuildPrompt, FilterTags (18 cases), checkContentTooLarge, parseTokenLimitError, parseInsufficientCreditsError (9 cases), NormalizeCore_Concurrent (8-goroutine `-race` regression guard for the shared-`transform.Chain` data race)
- ✓ Error classification tests (7 tests) — constructors (ENotFound/EConflict/EInvalid/EInternal), Error string, Unwrap/KindOf, FromDB mapping (nil/ErrNoRows/unique/other), PgError predicates
- ✓ Pool lifecycle tests (5 tests) — StartStop, ContextCancellation, DoubleStop, PanicRecovery_Restart (worker survives panic and restarts), PanicRecovery_StopDuringRestart (clean shutdown during restart delay)
- ✓ `internal/testutil` package — assertion helpers, PDF fixtures, mock embedder
- ✓ `Makefile` test targets (`make test` — 12 packages, `make test-db` — 6 packages, `make test-verbose`)
- ✗ No tests for CLI commands (kushim consume, search, task, setup)
- ✓ CLI config tests (config_test.go — parseValue, deleteNestedKey, configHandler integration) — requires CGo toolchain
- ✓ CLI migrate tests (migrate_test.go + configtask_migrate_nodb_test.go — DirChanged, ErrNoOp no-op contract, unsafe-path rejection, storage no-op) — no DB required

### Build & Deployment

- Two binaries: **kushim** (CLI) + **edub** (API server)
- `edub` built with `CGO_ENABLED=0` (pure Go, no C dependencies)
- `kushim` retains static CGo linking for MuPDF, Tesseract, Leptonica, libpng
- Pre-built `libtokenizers.a` for Hugot Go backend
- Containerized builds for glibc and musl (`make build-glibc`, `make build-musl`)
- Deployment image (`make build-tools-image`)
- **Docker Compose quick-start** (`docker compose up`) — multi-stage build from source, zero host-side toolchain required
- Go build tags: `XLA`, `ORT` for Hugot ONNX support
- `make web-build` target for UI embedding

---

## 🟢 Roadmap — Backlog

> **Core differentiator**: Unattended bulk classification with tag-vocabulary consolidation
> (tags, title, document type, people) and unattended recovery from wedged processing
> tasks — every document is classified without manual intervention, labels are normalized
> against the existing vocabulary via semantic matching + consolidation (rather than
> suggested one-by-one per document), and the system automatically detects, reclaims,
> and retries stuck workers without administrator attention.

> Entries are sorted out by decreasing estimated priority/urgency. Entries number IS NOT related with their ordering.

| #   | Feature                                                            | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Configuration/setup wizard in web UI**                           | ✓ In-app configuration page (LLM providers, Tesseract languages, storage paths, model downloads)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2   | **Continue interrupted tasks**                                     | ✓ Manual retry for failed tasks (API + web UI). Wizard auto-resumes on restart, re-submission does not duplicate tasks, failed config downloads surfaced with retry action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3   | **Docker Compose quick-start**                                     | ✓ Single `docker compose up` command runs the full stack — multi-stage Dockerfile builds everything from source, entrypoint handles first-boot setup and conditional apt installs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4   | **Document detail — tags/people/type**                             | ✓ Display tags, people, and document type in the sidebar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5   | **Document metadata editing**                                      | ✓ Detail page sidebar — edit tags, people, type, title (override LLM)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 6   | **Document update endpoint**                                       | ✓ `PUT /api/v1/documents/{id}` — update title, document\*type_id, text_content                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 7   | **Document delete endpoint**                                       | ✓ `DELETE /api/v1/documents/{id}` — remove document + files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 8   | **Document tag/people assignment**                                 | ✓ `POST/DELETE /api/v1/documents/{id}/tags` and `/people` — junction management                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 9   | **Tags CRUD API**                                                  | ✓ `GET/POST/PUT/DELETE /api/v1/tags` with `TagService` batch CRUD, auto-encoding, cache sync                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 10  | **People CRUD API**                                                | ✓ `GET/POST/PUT/DELETE /api/v1/people` + `/api/v1/people-types` — `PeopleService` + `PeopleTypeService` batch CRUD                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 11  | **Document Types CRUD API**                                        | ✓ `GET/POST/PUT/DELETE /api/v1/document-types` — `DocumentTypeService` batch CRUD                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 12  | **Tag management page**                                            | ✓ List, create, edit, delete tags with filter input + conflict detection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 13  | **People management page**                                         | ✓ Two-tab route: People (name, native name, cascade delete) and Person Types (name, description, 409 handling)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 14  | **Document type management page**                                  | ✓ List, create, edit, delete document types with 409 conflict handling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 15  | **Document upload/consume flow**                                   | ✓ Wire Upload button to `POST /api/v1/consume/upload`, show progress feedback via modal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 16  | **Single task detail page**                                        | ✓ New route `/tasks/{taskID}` — status badge, batch/doc links, timestamps, error display, retry action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 17  | **Integration test**                                               | ✓ End-to-end consumption test (mock runner), DB CRUD, search, task system, API handlers — covering database queries, search engine, pool, enrichment flow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 18  | **Test coverage**                                                  | ✓ Database queries, API handlers, search engine, task system (store/runner/dispatcher/pool), consumption pipeline, storage layer, orphaned service, errored file service and handler, LLM registry, config load/watcher, utils (text/logging), auth tokens/middleware/permissions, content analyzer (prompt building/tag filtering), error classification, worker pool lifecycle, password validation. ✗ Adapters, CLI commands (existing gap)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 19  | **Document download**                                              | ✓ Download individual documents from the document list table. Batch download with configurable limits (max file count and/or total accumulated size).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 20  | **Bulk operations**                                                | ✓ Batch delete, batch tag assignment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 21  | **Batch cancel API endpoint**                                      | ✓ `POST /api/v1/batches/{id}/cancel` — expose `kushim consume cancel` through the API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 22  | **LLM capability registry & dynamic config**                       | ✓ Replaced hardcoded per-provider LLM config (`llm.openai`, `llm.anthropic`, etc.) with a dynamic model selection system. New `internal/llm/` package with capability registry backed by a manually curated `model_catalog.json`. Flat `LlmConfig` struct with `adapter`/`provider`/`model`/`token`/`endpoint`/`reasoning`/`reasoning_effort`/`temperature` fields. Removed four provider adapters (`llm_openai.go`, `llm_deepseek.go`, `llm_ollama.go`) and consolidated into `openai-compatible`/`anthropic`/`custom` adapter system (Ollama adapter removed). `GET /api/v1/llm/models` discovery endpoint returns adapters, providers, and models with capabilities. Cascading selector UI (Adapter → Provider → Model) in settings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 23  | **Dashboard: storage panel**                                       | ✓ Total size, original type breakdown (count + size per type), cumulative storage trend by day/week, average file size, total pages, total words                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 24  | **Dashboard: batch overview panel**                                | ✓ Recent N batches with per-batch task summary, active/orphaned state, batch source, creation time, duration when complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 25  | **Dashboard: activity timeline**                                   | ✓ Replaced by the Active Tasks strip: chronological feed (document uploaded, task completed, task failed, batch created) was removed in favor of live `pending`/`processing`/`waiting` task cards (processing-first, cap 25) fed by `ListActiveTasks`; `running_tasks[]` with derived `label` replaces `activity[]` in the dashboard response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 26  | **Dashboard: document analytics panel**                            | ✓ Language distribution, document type distribution, tag frequency (top N), documents without tags/type/language counts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 27  | **Dashboard: processing health panel**                             | ✓ Task success rate (completed vs failed, last 7 days), avg task duration, active batch count, orphaned batch count, missing tools count                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 28  | **Dashboard: summary API**                                         | ✓ Single `GET /api/v1/dashboard` endpoint returning all panel data — built from GROUP BY/JOIN queries on existing schema, no migrations needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 29  | **User accounts**                                                  | ✓ User CRUD, bcrypt password hashing, user list page in settings UI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 30  | **Login and session management**                                   | ✓ Login/logout endpoints, JWT session tokens, auth middleware, protected API routes, login page, auth guard in root layout                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 31  | **Orphaned file management**                                       | ✓ Detect, quarantine, list, delete, restore, and re-ingest orphaned files via API, CLI (`kushim storage orphans`), and WebUI. Startup + post-batch auto-scan. Database-backed tracking table with soft-delete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 32  | **Batch service**                                                  | ✓ Create `internal/service/batch.go` — `BatchSummary`, `BatchOverview`, `CancelResult` domain types. All batch operations (GetSummary, ListSummaries, ListOverviews, CountOrphaned, RetryFailed, Create, BeginCancel/CompleteCancel, CountDistinct, ActiveIDs, HasPendingWork, IsLockedByLiveOwner, CountPausedBatches, ListPausedBatches) moved from handlers/helpers to the service. 16 service-level tests verify owner-state derivation, orphaned flag, cancel flow, paused-batch counting and listing. Dead code (`CountBatchStatuses`, `ListBatchSummaries`, `RetryBatchFailed`, `BatchOwnerState`) removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 33  | **Batch queue schema and queries**                                 | ✓ Migration adds `status` column to batch table with backfill, 13 new SQL queries (CountQueuedBatches, GetNextQueuedBatch, RequeueBatch, SetBatchProcessing/Completed/Failed/Cancelled/Paused, CountPausedBatches, ListPausedBatches, CountLiveBatches, ListStaleBatchOwners), sqlc regeneration, service wrappers, `CompleteCancel` sets `cancelled`. API response types include `Status`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 34  | **`kushim queue` daemon**                                          | ✓ New daemon CLI command (sibling to `kushim hugot`). Runs two loops: (a) queue consumption — `GetNextQueuedBatch`, fork `kushim consume --batch <id>` when `CountLiveBatches < MaxConcurrentBatches`; (b) stale reclamation — `ListStaleBatchOwners` (>15s heartbeat with active tasks), reset processing→pending, requeue, delete stale owner. `--bg` to daemonize. PID file, signal handling, zombie reaping, orphaned-batch recovery via pre-fork owner row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 35  | **Move inbox polling to `kushim queue`**                           | ✓ Remove `PollingScheduler` from edub (`internal/scheduler/`). Embed inbox scanning loop in `kushim queue` as a third loop. Before enqueueing: check `CountQueuedBatches + CountLiveBatches < MaxConcurrentBatches`. Respects existing `consumer.polling` config (interval, windows, enabled). Config reload moved to the queue daemon's 5s housekeeping ticker using `atomic.Pointer` — config changes propagate ~5s after saving to disk without a restart. `consumption.ScanAndEnqueue` shared function supports future handler/CLI unification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 36  | **API endpoints enqueue, no fork**                                 | ✓ `POST /api/v1/consume` and `POST /api/v1/consume/upload` create batch with `status='queued'`, return 202 with batch ID. Remove `forkWorker()` and all semaphore acquire/release from `ConsumeHandler`. Remove `semaphore` field from handler and constructor. Remove semaphore creation and `registerRoutes` parameter from `server.go`. Inbox scanning and dedup unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 37  | **API batch resume enqueues**                                      | ✓ `POST /api/v1/batches/{id}/resume` sets batch status to `queued`, resets processing→pending tasks. Queue daemon picks it up instead of direct forking.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 38  | **Delete `pool.Semaphore`**                                        | ✓ Remove `internal/pool/semaphore.go`. No remaining call sites after features 34–36. `MaxConcurrentBatches` config stays for queue daemon (reads via `kushim queue` from shared config).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 39  | **CLI `kushim consume` enqueues with direct-fallback**             | ✓ Scans inbox, creates batch with `status='queued'`. If queue was empty before enqueueing, falls back to direct processing (current behavior). If queue was non-empty, prints "Batch <id> queued — start `kushim queue` to process". Remove `--bg` flag. `--batch <id>` unchanged for recovery.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 40  | **Upgrade MuPDF to 1.28.0**                                        | ✓ Rebuild kushim against MuPDF 1.28.0 (which includes Bug 708689 fix + broader `fz_var` initialization), rerun the crash-test on all 4 known files. If the crash disappears, the upstream fix is confirmed — no filing needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 41  | **Isolate `PdfCleanFile` in a subprocess**                         | ✓ Add a hidden subcommand (e.g. `kushim internal-mupdf-clean`) that calls `PdfCleanFile` and exits. Have `mupdf.go`'s `Optimize()` shell out to it via `exec.CommandContext`, so a SIGSEGV kills only the subprocess and returns a normal Go error — making the Ghostscript fallback (`runner.go:205-227`) reachable for these files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 42  | **Cap retry count on batch task reset**                            | ✓ `ResetProcessingTasksByBatch` now quarantines tasks to `failed` after `max_retries` consecutive reclamation cycles. Per-task `attempts` counter with configurable `consumer.reclaim.max_retries` (default 3). Counter resets on successful completion or explicit user retry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 43  | **Signal orphaned children in stale reclamation**                  | ✓ `reclaimStaleBatches` signals (SIGTERM) the previous owner's PID before resetting tasks, matching `consumeCancelHandler`'s existing pattern. `ListStaleBatchOwners` query extended to return `owner_id` and `pid`. This bounds resource pileup for any future cause of staleness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 44  | **Orphan Restore creates proper batch**                            | ✓ `Restore()` creates batch record with `status='queued'`, creates matched consume+enrich task pair (fixes missing enrich task bug). No direct forking — queue daemon picks it up. `MoveToInbox` path unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 45  | **Auto-detect OCR languages from LLM**                             | ✓ LLM-detected document language from enrichment is cross-checked against configured OCR languages. Missing languages are added to config automatically, improving OCR accuracy without manual intervention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 46  | **Setup creates initial admin user**                               | ✓ `kushim setup` (CLI + wizard) prompts for admin username + password before completion, creates user via `service.User.Create()`. Session secret auto-generated and persisted during setup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 47  | **API keys**                                                       | ✓ Key generation/revocation/rotation, Bearer token validation middleware, per-user key management. SHA-256 hashed storage with `ek*`prefix,`AuthSourceKey`context value for audit logging.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 48  | **Roles and permissions**                                          | ✓ RBAC schema with admin/editor/viewer roles, permission enforcement middleware, role assignment, self-service`/me`endpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 49  | **Role-based UI**                                                  | ✓ Show/hide UI elements by role (admin-only: Users page, Config page, Logs; editor-only: Upload, batch actions, delete buttons; viewer: read-only). Role badge in user list. Role dropdown in user create/edit forms. Self-service profile page with API key management and role display. Frontend reads role from`GET /api/v1/me`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 50  | **Enrich single document**                                         | ✓ "Re-enrich" action in the document detail view (WebUI) and a `kushim enrich <document-id>` CLI command to trigger enrichment for a single existing document. Useful for documents that completed consumption but failed at enrichment, or when the user wants to re-run classification after editing metadata or adjusting tags. Creates a single enrich task (`status='pending'`, dedup key `enrich:doc:<uuid>`) in a queued `"reenrich"`batch. Enrich-only batches skip the external-tools guard so they work on hosts without OCR/PDF engines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 51  | **LISTEN/NOTIFY queue consumption**                                | ✓ Replaced the 5-second polling ticker for batch consumption with Postgres`LISTEN`/`NOTIFY`. A database trigger (`notify*batch_queued`) on the `batch`table fires`pg_notify('batch_queued', NEW.id)`on every INSERT/UPDATE that sets`status='queued'`. A dedicated pgxpool connection (MinConns=1, MaxConns=1) in a background goroutine listens for these notifications and signals the main loop to run `consumeNextQueuedBatch`immediately (milliseconds). A 30-second safety timer guarantees no batch is stuck >30s. Backup scheduling, stale batch reclamation, and stale task reclamation remain on the 5-second housekeeping ticker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 52  | **Research: handle gosseract TIFF warnings as non-fatal**          | ✓ gosseract OCR exits with status 2 and prints`pixReadMemTiff: function not present`to stderr when Tesseract is built without TIFF support. These warnings are harmless (OCR output is still produced) but currently cause the task to fail and the document to be moved to`storage/errors/`. Investigate whether exit code 2 with known stderr patterns should be treated as partial success — the pipeline should continue with whatever OCR text was produced. If confirmed, filter stderr for known non-fatal patterns or distinguish exit code 2 from true errors in the gosseract adapter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 53  | **Pre-request LLM token budget check**                             | ✓ Enrichment fails with a 400 error when the LLM prompt exceeds the model's context window (e.g. 306774 tokens vs 262144 max for Gemini). TextRank reduction targets a word count but token count depends on the document language and the prompt template overhead. Added a fast character-level token estimator (`EstimateTokens`) that detects CJK vs Latin text with a conservative blended formula. Both adapters (`openai-compatible`, `anthropic`) check the estimate before making any API call and return a `ContentTooLargeError`if it exceeds`MaxInputTokens`. The enricher catches this error, computes a proportional word-target reduction, re-runs TextRank, and retries once (up to 2 total attempts). A response-time `TokenLimitError` is also parsed from provider error bodies (`parseTokenLimitError` with regex for OpenAI/DeepSeek format) and follows the same retry path — a belt-and-suspenders fallback for when the heuristic underestimates. Falls through to a clear "document too large for model X/Y" error if the document can't be reduced enough.                                                                                                                                                                                                                       |
| 54  | **Detect LLM provider credit/balance errors**                      | ✓ Detect when the LLM provider returns an error indicating insufficient credits or billing issues. The affected batch is auto-paused (`status='paused'`). New batch creation is blocked (both via CLI `kushim consume`and the`kushim queue`polling daemon) until the user resolves the billing issue and resumes or cancels all paused batches.`CountPausedBatches`and`ListPausedBatches`SQL queries + service methods detect paused batches; guards in`ScanAndEnqueue`and`consumeHandler`enforce the block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 62  | **Research: LLM provider capability discovery**                    | ✓ Investigate how each supported provider (OpenAI, Anthropic, DeepSeek, Mistral/Ollama) exposes model capabilities — available models, supported request parameters, allowed enum values per parameter, and how they differ from the OpenAI standard. Map the compatibility surface to inform adapter design and avoid 400 errors from unsupported fields.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 63  | **Two-pass LLM document type refinement**                          | ✓ Document type is refined via a second LLM pass using head+tail of the raw full text (the structural markers TextRank drops). Pass 2 sends the full conversation history (system + user prompt + pass-1 assistant response) for provider caching.`ExtractHeadTailWords` captures opening/closing sections. Graceful fallback to first-pass type on error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 64  | **Send document metadata to type refinement pass**                 | ✓ Include useful document metadata (page count, total word count, file size) as additional context in the second LLM pass for document type refinement. Pass the metadata alongside the head/tail text so the LLM has richer structural information about the document, improving classification accuracy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 65  | **Secure wizard config endpoints behind admin role**               | ✓ Wizard configuration endpoints (`GET/PUT /wizard/config`, `GET /wizard/config/status`, `POST /wizard/config/retry`) previously bypassed auth entirely. Now require admin role. A new public `GET /wizard/bootstrap` endpoint exposes only non-sensitive fields (`auth_enabled`, `missing_tools`) so the SPA can decide whether to show the login screen without leaking LLM tokens or DB connection details. JWT validation hardened with explicit `WithValidMethods`HS256 restriction and SameSite=Lax on logout cookie.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 88  | **Makefile CGo test target**                                       | ✓`make test-cgo`(host) /`test-cgo-glibc`/`test-cgo-musl`(containerized) run 10 CGo-constrained tests across 3 packages:`internal/tools/adapters`, `internal/tools/adapters/ocr`, `internal/tools/adapters/tagmatcher`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 77a | **Ingest images (PNG, JPEG, TIFF)**                                | ✓ Wire raster image formats into the consumption pipeline. The OCR path already exists for image-based content (MuPDF page render + Tesseract) but the inbox scanner and file detection currently restrict `consumer.supported_files`to`.pdf`. Extend the MIME-type allowlist and file-extension checks to accept `image/png`, `image/jpeg`, and `image/tiff`, routing them through the same gosseract OCR adapter for text extraction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 77b | **Ingest word-processor documents (DOCX, ODT)**                    | ✓ Add DOCX (Office Open XML) and ODT (OpenDocument) to the supported file types. Implement text extraction by parsing the internal XML structure of each format (ZIP container with `word/document.xml`or`content.xml`) rather than shelling out to an external office suite. Wire the extracted text into the existing consumption pipeline (text storage, indexing, enrichment) and ensure the originals are preserved in storage alongside the extracted content.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 78  | **Docker Compose with bundled PostgreSQL**                         | ✓ The current compose file requires an externally-provided database plus a mounted config pointing at it. Bundle a PostgreSQL 17 container and wire the first-boot entrypoint to configure it automatically, removing the external-database dependency for quick-start setups. Added `docker-compose.quickstart.yml`override (db service, health-gated`depends_on`, `edub-db-data`volume),`DB_DSN`/`ADMIN_USER`/`ADMIN_PASSWORD`env forwarding in`docker/entrypoint.sh`, `--db-dsn`flag on`kushim setup --cli`(persisted as`database.dsn`), and `make compose-quickstart`/`compose-quickstart-down` targets. External-DB mode (`docker compose up`) unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 57  | **Pre-built binaries**                                             | ✓ GitHub Actions `release.yml`builds both binaries per arch (amd64/arm64, natively):`kushim`(CGO_ENABLED=1, C deps estáticas vía`make build-deps`con cache) y`edub`(CGO_ENABLED=0, SPAs embebidas desde el job`web`). Empaqueta 4 tarballs + `checksums.txt`y publica GitHub Release: publicada en tag push, draft en`workflow_dispatch`con`publish: true`. `download-tokenizers`parametrizado por arquitectura con verificación sha256.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 80  | **Trash / soft delete**                                            | ✓ Deletion currently removes the row and both storage files with no recovery path. Add a`deleted_at`timestamp column, filter deleted documents from queries, and provide a trash view for restore or permanent delete. Implemented:`deleted_at`column (migration 00007), soft-delete moves files to`<storage>/trash/`, trash API (list/detail/restore/permanent-delete/batch/purge), `storage.trash.retention_days`(default 30) with hourly background purge + manual purge endpoint, settings UI Trash section,`/trash`web page (list, restore, permanent delete, purge).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 91  | **Configurable delay after each LLM request**                      | ✓`enricher.contentanalyzer.llm.request_delay`(seconds, default 1, 0 = off, max 60) sleeps after every LLM HTTP request in both adapters — classification and doc-type refinement passes, every retry — via a shared context-aware helper that aborts early on batch cancellation. Exposed in the web UI settings page under Content analyzer / LLM; config validation rejects negative values and values above 60; the settings save endpoint validates before persisting; the content-analyzer timeout is now actually applied to bound the sleep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 92  | **Research: full-suite test failures**                             | ✓ Root causes:`TestConfigHandler_Unset`unset a key that its fixture never set (missing-key → error is intentional, pinned by`TestConfigHandler_UnsetMissingKey`), and the DOCX/ODT page-count assertions depended on MuPDF availability (`countPages`returns 0 without it, 1 with it). The package was never compiled because`internal/commands`was in no make target and`hugot.go`referenced CGo-only`tagmatcher`symbols. Fixes: set-then-unset flow in the test; injectable`pageCounter`on`Consumer`(defaults to real`countPages`, stubbed to 1 in consumption tests, assertions now `PageCount == 1`); `internal/commands`added to`make test-cgo`— tested under CGo where it ships, no stub and no build-tag contortions; new`make test-cgo-db`target runs consumption under CGO_ENABLED=1 + DB; new`test-cgo`CI job covers both. Full suite is now green end-to-end.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 76  | **Auto-run database migrations when connection settings change**   | ✓ When the user changes any`database.*`connection parameter via`PUT /wizard/config`, the backend now defers the switch: it enqueues a `migrate-db`config task (dedup key`config:migrate-db`) instead of saving the new settings. The task acquires the backup lock, waits for in-flight tasks to drain, takes a best-effort pre-migration snapshot (`backup.path`, retention-capped), dumps the current database (SQL dump incl. `goose*db_version`preservation), validates the destination (refuses databases with tables but no edub migration history; skips when the destination already holds data), restores it statement-by-statement, and only then persists the new connection settings to`config.yaml`. Both `edub`(config watcher) and`kushim queue`(5s housekeeping ticker) detect the config change, reconnect to the new database (probe first, keep old config on failure), and restart`LISTEN`/`NOTIFY`. Non-DB settings from the same PUT are persisted immediately; storage dir changes are handled by the separate `migrate-storage`task (see #95), which runs after`migrate-db`when both change. A failed task is superseded atomically (status-guarded delete) on the next save; retries are idempotent (destination data-presence check).                                            |
| 95  | **Storage path migration**                                         | ✓ Changing`storage.storage_dir`or`storage.consumption_dir`(settings UI or direct config edit) now enqueues a`migrate-storage`config task (dedup key`config:migrate-storage`) that acquires the backup lock, drains in-flight tasks, rewrites pending consume-task `file_path` payloads, moves the storage subdirs (`processed/`, `originals/`, `errors/`, `orphaned/`, `trash/`, `thumbnails/`) and inbox files per `storage.migration_mode` (`copy`— copy-then-delete, default — or`move`— rename with cross-filesystem copy fallback), rewrites`document.storage_path`/`original_path`and`orphaned_file.file_path`, and only then persists the new dirs to `config.yaml`. The `migrate-db`task no longer touches storage paths; combined DB+storage changes enqueue`migrate-db`first, then`migrate-storage` (single-worker FIFO config pool, so the storage task connects to the new database).                                                                                                                                                                                                                                                                                                                                                                                                          |
| 96  | **`kushim migrate`CLI command**                                    | ✓`kushim migrate <database&#124;storage>`exposes the migration logic from the CLI instead of only through the config task queue.`kushim migrate database --host <h> --port <p> --user <u> --password <p> --database <db> [--sslmode <mode>]`copies the current database into the new one exactly like the`migrate-db`config task: backup-lock gate, in-flight drain, pre-migration safety snapshot, dump/restore with goose version preservation, destination validation, and deferred config.yaml update (the password may also come from`KUSHIM_DB_PASSWORD`). `kushim migrate storage --storage-dir <new> [--consumption-dir <new>]`moves files to the new paths and rewrites the database paths (roadmap #95), rejecting paths under system locations and reporting a no-op when nothing changes. Both subcommands persist the new settings to`config.yaml`only after the operation succeeds.                                                                                                                                                                                                                                                                                                                                                                                                          |
| 94  | **Optional fallback LLM provider/model**                           | ✓`enricher.contentanalyzer.fallback.{enabled,llm.*}`— a second, independent LLM config (adapter/provider/model/token/temperature/request_delay, same shape as the primary`llm`block) built as a second`ContentAnalyzer`in the Runner. When a primary call fails with a provider-attributable error (API down/unreachable, credit or rate-limit, malformed/unparseable response, provider-side timeout),`AnalyzeContent`/`AnalyzeDocType`retry the same request through the fallback. Request-side failures never trigger it: pre-request token-budget`ContentTooLargeError`, response-time `TokenLimitError`, and batch lifecycle errors (`context.Canceled`/`DeadlineExceeded`). Disabled by default (no fallback adapter is built). Interaction with pause-on-credit-error (#54): the batch pauses only when BOTH primary and fallback fail with a credit error (`credErr.Provider` now names the actual failing provider); if the fallback succeeds, enrichment proceeds normally. Retry attempts (#53) and the request delay (#91) apply per attempt against the active provider. Validation (`finalizeConfig`) requires adapter/provider/model and bounds `request*delay` only when the block is present and enabled. Exposed in the settings UI as a "Fallback LLM" sub-card under Content analyzer. |
| 97  | **Fix file size rendering on document detail page**                | ✓ The document detail page (`web/src/routes/documents/[id]/+page.svelte`) hardcodes `\u00A0KB`directly in the Svelte markup, where`\u00A0`is not interpreted as a JS escape, so it renders literally as "978\u00A0KB" instead of "978 KB". It also always divides by 1024, so files ≥ 1 MB show the wrong unit ("1536 KB" instead of "1.5 MB"). Replace the inline formatting with the shared`formatSize()`helper from`web/src/lib/utils/html.js`, which emits a real non-breaking space and handles B/KB/MB/GB.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 87  | **Backup mode selection**                                          | ✓ Per-mode backups: `backup.schedules` config list — one entry per mode (`full`/`database`/`documents`) with its own interval, preferred time, keep count, and optional output path; legacy flat `backup.interval`/`time`/`keep`auto-convert to a single`full`schedule (conflicting flat+schedules configs are rejected).`kushim backup --mode <full                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | database | documents>` for manual runs (no retention applied); scheduled backups capture mode/path/keep in the task payload and prune only their own mode's archives (`edub-backup-<mode>-\*.tar.gz`, plus legacy no-mode archives under `full`). Restore is mode-aware: `documents`archives skip the SQL dump and path rewrite,`database`archives skip the storage swap, and legacy manifests without`mode`restore as`full`. Archive naming and per-mode dedup keys (`backup:<mode>:<date>`). Settings page exposes an add/remove/edit schedule list. |
| 79  | **Thumbnail generation and document grid view**                    | ✓ Thumbnails rendered from the first page of processed documents (or the source image) via a linked `thumbnail`task activated after each successful consume — subprocess-isolated through`kushim internal-thumbnail`(MuPDF render at 72 DPI, default) behind a pluggable`Thumbnailer`adapter. Stored as JPEG in`storage/thumbnails/<date>/<uuid>.jpg`(fixed 3:4 crop,`consumer.thumbnails.*`config: enabled/engine/dpi/max_width/quality/timeout/workers).`has*thumbnail`column on`document`(migration 00008), exposed as`has_thumbnail`in document/search API responses, served via`GET /api/v1/documents/{id}/thumbnail`. New grid/tile view on the documents page toggles alongside the DataTable; the preference persists in `localStorage` (`edub:documents:view`) pending the #58 user-preferences system — no URL param. Lazy-loaded thumbnails with type-icon fallback, selection, batch actions, shared URL-synced pagination/sort. Task lifecycle integrated: gated during backups, discarded with failed parents, restored on retry, counts toward drain.                                                                                                                                                                                                                                       |
| 98  | **Thumbnail integration with trash system**                        | ✓ `MoveToTrash`/`RestoreFromTrash`gained a`thumbnailPath`parameter (empty string skips thumbnail handling) and move the thumbnail to/from`<trash>/<document_id>/thumbnails/<basename>.jpg`, mirroring the `originals/`/`processed/`layout; restore recreates the date-based path under`storage/thumbnails/`(with`MkdirAll`) and `removeEmptyTrashDirs`also removes the trash`thumbnails/`subdir.`SoftDelete`passes the path only when`has_thumbnail`is set and`created_at`is valid;`RestoreDocument`and`PermanentlyDelete`derive it from`created_at`(skip-if-missing tolerates pre-feature trashed documents, and permanent delete best-effort removes the live thumbnail for those).`PurgeExpired`runs a new`cleanupOrphanedThumbnails`sweep whenever the purge deleted rows: it walks`storage/thumbnails/`, collects docIDs from filenames, checks them against the DB with a batched `IN` query (`documentIDsExist`, 1000 IDs/batch), removes orphans, prunes emptied date dirs, and aborts early on context cancellation. No schema or API changes.                                                                                                                                                                                                                                                    |
| 99  | **Thumbnail integration with backup system**                       | ✓ The generic whole-dir implementations already covered thumbnails: `createTarGz`archives the entire`storage/`tree (so`--mode full`/`documents`include`storage/thumbnails/`), `ReplaceFiles`swaps the whole extracted tree back on restore, and`moveDirEntries`relocates every top-level subdir (so`migrate-storage`moves`thumbnails/`alongside the rest). Thumbnail paths are derived at runtime from`storage_dir`+`created_at`+`docID`, not stored in the DB, so no path-rewrite clause was needed. Work done: centralized storage subdir names as `storage.Dir*` constants (`internal/storage/dirs.go`) and replaced raw literals across `internal/storage`, `internal/consumption`, and `internal/service`; added backup/restore/migrate test coverage proving thumbnails survive each path; updated docs to name `thumbnails/`explicitly.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 100 | **Orphaned thumbnail cleanup**                                     | ✓`kushim storage thumbnails cleanup [--dry-run]`CLI subcommand plus an ungated hourly sweep:`TrashService.CleanupOrphanedThumbnails`(public) walks`storage/thumbnails/`, collects docIDs from filenames, checks them against the DB with a batched `IN` query (`documentIDsExist`, 1000 IDs/batch), removes orphans, prunes emptied date dirs, and skips files modified within the last 30s (recency guard). `PurgeExpired`now runs the thumbnail sweep every cycle regardless of retention deletes;`cleanupOrphanedTrashDirs`stays gated on`deleted > 0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 108 | **Allow adding N number of fallback providers for LLM enrichment** | ✓`enricher.contentanalyzer.fallback`(single block) became`enricher.contentanalyzer.fallbacks`— an ordered list of`{enabled, llm.*}`entries (no hard cap). The Runner builds one`ContentAnalyzer`per enabled entry and, on a provider-attributable error (`isProviderError`), retries the request through each fallback in order until one succeeds; if all fail, the last error surfaces (so the batch pauses on credit errors only when the primary and every enabled fallback end in a credit error). Request-side failures (`ContentTooLargeError`, `TokenLimitError`, `context.Canceled`/`DeadlineExceeded`) still short-circuit with no fallback tried. Validation (`finalizeConfig`) requires adapter/provider/model and bounds`request_delay`per enabled entry, indexed as`fallbacks[i]`. Settings UI renders an add/remove/reorder list with per-row enable toggle, mirroring ocr languages / polling windows / backup schedules. Legacy single-`fallback:`configs are silently ignored (feature is off-by-default; no migration code).                                                                                                                                                                                                                                                            |
| 101 | **Backfill thumbnails for existing documents**                     | ✓ `kushim thumbnails <mode>` enqueues `pending` thumbnail tasks for any document with `has_thumbnail = FALSE AND deleted_at IS NULL` (no `waiting_for`), reusing the task retry system. Modes: `--all` (paginated 500/page), `--batch <id>` (documents whose completed `consume` task is in that batch, via `payload->>'document_id'`), `--document <id>` (single). Enqueues into a `queued` batch (`source = "thumbbackfill"`); dedup key `thumbnail:doc:<uuid>` (unique partial index) skips already-pending documents in bulk modes and surfaces as a conflict for `--document`; `--force` overrides the `consumer.thumbnail.enabled` gate. Service: `ThumbnailBackfill` (`internal/service/thumbnail_backfill.go`); queries in `document.sql` (`ListDocumentsWithoutThumbnails*`, `GetDocumentWithoutThumbnail`). Scheduled mode: `consumer.thumbnail.backfill_interval` (days, 0 = disabled) + `backfill_time` (HH:MM) make the `kushim queue` housekeeping ticker fork `kushim thumbnails --all` when due — first run anchored to `backfill_time`, last run seeded from the most recent `thumbbackfill` batch `created_at` (survives restarts), skipped while a backfill batch is queued/processing; sub-daily intervals supported. Keyset pagination (`id > $1` over the partial index `idx_document_no_thumbnail`, migration 00009); tasks created before the batch row are deleted on interrupted runs (deferred cleanup). Settings UI exposes both fields. Single-document regeneration via API tracked as #111.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 102 | **Remove rule-suppression comments from Svelte files**             | Eliminate all `eslint-disable-next-line`/`svelte-ignore`comments in`web/src`(12`eslint-disable`occurrences across 6 files: DataTable, DocumentGrid, document-types, people, logs). Two rule classes:`svelte/prefer-svelte-reactivity`(mutating built-in`URLSearchParams`) → migrate to `SvelteURLSearchParams`/`SvelteMap`/`SvelteSet`from`svelte/reactivity`; `svelte/no-at-html-tags` (`{@html}`) → replace with inline SVG blocks, `<svelte:element>`, or escaped component markup. Where raw HTML injection is genuinely required (DataTable's caller-built HTML cells), restructure the component to accept Svelte nodes/snippets instead of HTML strings so `{@html}`can be removed rather than suppressed. Goal: zero rule-suppression comments in the frontend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 86  | **Dashboard: daily storage increase chart**                        | ✓ Frontend-only: the dashboard already returns per-day`daily_bytes`in`storage_trend[]`, so no backend/API change was needed. `StoragePanel.svelte`now renders a "Daily Storage Increase" SVG bar chart beside the cumulative trend in a`lg:grid-cols-2`grid (stacks on small screens), with its own ResizeObserver sizing, an independent y-axis scaled to`daily_bytes` (`dailyMax`/`dailyYTicks`, `formatSize` labels), lapis bars (`#3b5e8a`), native `<title>` tooltips (`formatSize · daily_count docs`), and the shared x-label skip logic (single-day datasets render one centered bar; the existing empty-state guard is unchanged). `daily_bytes`is gross adds per day, consistent with the cumulative chart.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 93  | **LLM token usage panel**                                          | Surface LLM token consumption in the web UI. Enrich tasks already store per-call usage (prompt/completion/total tokens) as JSON in`task.result`and log it; add a dashboard panel or dedicated`GET /api/v1/llm/usage` endpoint aggregating tokens per model/provider over time (daily/weekly), optionally with estimated cost and per-document/per-batch breakdowns, so administrators can track LLM spend alongside the existing token-budget checks and pause-on-credit-error behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 103 | **Fix people search results not shown in documents view**          | ✓ In the web UI documents view, searching for a person does not show the list or search results.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 104 | **Fix autocomplete crash in documents view search bar**            | ✓ The autocomplete envelope unwrap was fixed (`6c98806`); this entry additionally hardened the search bar and FilterPanel: autocomplete fetches go through a strict helper (`requestStrict`+`asList`in`web/src/lib/api.js`) that throws instead of returning `null`, failures surface as error toasts and can never leave an unhandled rejection or a permanently empty dropdown, and free-text people suggestions now use `person:<name>`, matching a person across all their document relationship types (backend reserved sentinel `PersonAnyType`in`internal/database/structured_search.go`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 55  | **Document notes/comments**                                        | User-added notes and annotations on documents                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 61  | **Contributing guide & issue templates**                           | `CONTRIBUTING.md`, issue/PR templates, and GitHub community setup to encourage contributions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 56  | **Native structured logging via slog**                             | Migrate from the adapter wrapper (`utils.Logger`with`reqID *string`+ Printf) to native`slog.Logger`interfaces. Replace`reqID *string`with context-bound loggers via`slog.Logger.With()`, add `InfoContext`/`ErrorContext`/`WarnContext`/`DebugContext`throughout. Enables fully structured key-value attributes at every call site (instead of format strings), direct integration with log aggregators (Loki/ELK), and eliminates ~257`reqID` parameter references across the codebase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 58  | **User preferences**                                               | Theme, pagination defaults, notification settings. Documents view mode (table/grid) is currently a localStorage toggle (`edub:documents:view`on the documents page) as a stopgap — migrate to server/user preferences here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 59  | **Email ingestion**                                                | IMAP inbox scanning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 60  | **Document relationships**                                         | Parent/child, cross-references between documents                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 110 | **Automatic re-enqueue of manually retried batches and tasks**     | Retrying a failed batch or task resets it to `pending`, but the batch is not re-queued, so the queue daemon never picks it up without a manual `resume`. Add an opt-in setting (mirroring the auto-reclaim toggle) that automatically re-queues the batch when a manual retry leaves pending work, so retried batches and tasks are processed without manual intervention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 81  | **Per-document ownership and ACLs**                                | Roles exist (admin/editor/viewer) but the document table has no owner column, so every user sees every document. Add an`owner_id`column, scope queries to accessible documents, and optionally support shared-with / team-level access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 84  | **Research: benchmark large-collection performance**               | Establish whether the single-store PostgreSQL approach (tsvector + GIN + structured filters in one query) still holds an advantage on combined text+metadata queries at 10k–100k+ document scale. Publish benchmark results.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 85  | **Pluggable tag matcher adapters**                                 | The Matcher/Embedder/EmbeddingStore interfaces exist in`internal/tools/adapters/tagmatcher/`but only the Hugot implementation is wired. Add an API-based embedder backend to validate the adapter contract and make tag matching truly switchable. Any alternative backend must match the current model's multilingual coverage and long-context window (BAAI/bge-m3: 100+ languages, 8192 tokens).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 89  | **Granular restore flags**                                         | Add`--database-only`, `--files-only`, `--config-only`, `--overwrite-config`flags to`kushim restore`for partial restores and explicit config overwrite control.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 90  | **Commit signatures en`master` (hecho)**                           | Configurado: firma SSH de commits (`commit.gpgsign true`), CI gate (`required_status_checks` `test`/`web`) añadido al ruleset `global`, y flujo de trabajo `dev`→`master`vía PRs con merge commits (git-flow), eliminando`required_linear_history` para que ambas ramas converjan. El bypass de administración se conserva a propósito porque el requisito de 1 aprobación no se puede satisfacer en un proyecto en solitario.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 105 | **Case-insensitive people search on document detail page**         | The people autocomplete on the document detail page (`GET /api/v1/people?q=`) matches names with a case-sensitive `LIKE` prefix pattern (`SearchPeopleByNameWithDocumentCount`—`WHERE p.name LIKE $1`with`prefix + '%'`), so typing a differently-cased substring ("garcía" vs "García") returns no suggestions. Switch the match to `ILIKE`(or compare normalized/lowercased forms) so people search is case-insensitive, in the detail-page autocomplete and any other`people`autocomplete callers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 106 | **Create/save new people from document detail page**               | The document detail page's People section can only assign existing people via autocomplete; when the typed person does not exist yet, there is no way to create them. Add an inline create flow (name, optional native name, person type) that calls`POST /api/v1/people` and assigns the new person to the document in one step.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 111 | **Regenerate thumbnail button on document detail page** | Add a "Regenerate thumbnail" button to the document detail page (`web/src/routes/documents/[id]/+page.svelte`), mirroring the existing "Re-enrich" button (line 446–454). Backend: `POST /api/v1/documents/{id}/thumbnail` handler in `internal/api/handlers/document.go` (same shape as `ReEnrich` at line 677), registered in `internal/api/server.go` alongside the existing `GET /api/v1/documents/{id}/thumbnail` route, gated by `RequireRole(editor...)`. It calls the `ThumbnailBackfill.BackfillDocument` service method (from this plan) to enqueue a single `pending` thumbnail task into a `queued` batch. Frontend: `api.documents.regenerateThumbnail(id)` in `web/src/lib/api.js` (next to `reenrich` at line 121), `handleRegenerateThumbnail()` in `+page.svelte` with a `regenerating` state flag and success/error toast, button placed beside "Re-enrich" in the action area. Supports both initial generation (`has_thumbnail = FALSE`) and regeneration (overwrite existing file — `MoveFile` already overwrites, `SetDocumentHasThumbnail` is idempotent). For regeneration the dedup key `thumbnail:doc:<uuid>` may collide with an existing pending task; the endpoint should return 409 Conflict in that case so the UI can inform the user a task is already queued. Depends on the backfill service (#101) for the enqueue logic. |

---

## 🔵 Icebox

Low-priority items worth revisiting when nothing more impactful demands attention.

| #   | Feature                                                        | Description                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Double tokenization in chunked encoding**                    | `Encode()` and `encodeChunked()` redundantly tokenize the same text; per-chunk decode→re-encode round-trip adds unnecessary overhead. See [report](research/double-tokenization-chunked-encoding.md).                                                                                                                                                                                                        |
| 2   | **External search engine**                                     | ZincSearch or Meilisearch integration for large-scale search                                                                                                                                                                                                                                                                                                                                                 |
| 3   | **Metrics and monitoring**                                     | Prometheus endpoints, structured metrics                                                                                                                                                                                                                                                                                                                                                                     |
| 4   | **Post-classification notification**                           | Optional webhook or websocket event when a classification batch completes. Deferred: lacks auth, multi-tenancy, and notification infrastructure. A long-polling wait endpoint is a preferable first step.                                                                                                                                                                                                    |
| 5   | **Deterministic fast path to skip LLM on recurring documents** | Skip LLM enrichment on recurring documents (invoices, statements, forms) via content-hash, rule, or similarity match against already-classified documents with inherited metadata. Research complete — see [report](research/recurring-documents-fast-path.md).                                                                                                                                              |
| 6   | **CLI-driven migrations from UI/config edits**                 | Evaluate calling the new `kushim migrate` CLI commands for migrations triggered from the web UI settings or from directly editing `config.yaml`, instead of (or in addition to) the in-process `migrate-db` config task — unifies the migration path and makes the UI trigger and the CLI trigger behave identically.                                                                                        |
| 107 | **ImageMagick thumbnail engine adapter**                       | Implement the `imagemagick` thumbnail adapter behind the existing `config.Thumbnail.Imagemagick` constant. Today `internal/tools/adapters/thumbnail/adapter.go:28-29` returns "not implemented yet", `tools.go:243-244` already probes the binary, and `AvailableEngines["thumbnail"]` only exposes mupdf — so once the adapter lands, add the engine to that map to surface it in the settings UI dropdown. |

---

## See Also

- [Architecture & Design](architecture.md) — Core design principles and pipeline narrative
- [Tag Matcher Guide](tag-matcher.md) — Matcher config, memory, and CPU tuning
- [Code Reference](reference/overview.md) — Detailed implementation reference per package
- [Testing Reference](reference/tests.md) — Test infrastructure, patterns, and how to run
