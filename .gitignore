# dev directory
dev/
docs/dev

# editor directories and files
.vscode/
.kilo/

# configuration files (except example)
config.yaml
config.prod.yaml
!config.example.yaml

coverage.out

internal/static/build/*
internal/wizard/static/*

# local-only agent instruction addendum (loaded via .kilo/kilo.jsonc, never committed)
AGENTS.local.md
